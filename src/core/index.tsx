import React, {
  forwardRef,
  useRef,
  useState,
  useEffect,
  useMemo,
  useImperativeHandle,
} from 'react';

const ZrsPlayer = function ZrsPlayer() {

  return (
     <div></div>
  );
};

export default ZrsPlayer;